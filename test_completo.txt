// ==========================================
// TEST INTEGRAL: ANALISIS DE DATOS
// ==========================================
int data[10]
int i
int total
int max
int temp
int modo

// 1. Inicialización con FOR y Aritmética
// Rellenamos con cuadrados: 0, 1, 4, 9...
for i in 0..9 do
    data[i] := i * i
done

// 2. Modificación condicional con IF-ELSE y Operadores Lógicos
// Si el valor es par Y mayor que 10, lo ponemos a negativo
i := 0
while i < 10 do
    temp := data[i]
    if (temp % 2 == 0) and (temp > 10) then
        data[i] := 0 - temp
    else
        data[i] := temp + 1
    fi
    i := i + 1
done

// 3. Calculo con DO-UNTIL y SWITCH anidado
// modo 1: Sumar, modo 2: Buscar Maximo (simulado)
modo := 1
total := 0
i := 0

do
    switch modo {
        case 1:
            // Sumar todo el array
            temp := data[i]
            total := total + temp
        case 2:
            // Placeholder para otra lógica
            total := total
        default:
            total := 0
    }
    i := i + 1
until i == 10

// 4. Salida
// Imprimimos el total calculado
total
